# Dados baixados: reconstruíveis, pesados, e sujeitos a retificação pela CVM.
# O manifesto de hashes dentro do ranking.json é o que prova o que foi lido.
dados/

# `saida/` É versionada. O enunciado pede o ranking.md no repositório, e um
# entregável que só existe depois de alguém rodar o pipeline não está entregue.
# Quem clona vê o resultado; quem quiser conferir roda:
#   uv run ranking --reference-date 2025-12-31 --validate

# O enunciado do case é documento da Decade, não do projeto
*.pdf

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Editor / SO
.vscode/
.idea/
.DS_Store
Thumbs.db

# OneDrive
*.tmp
~$*

# Material for the video, which is not part of the delivery.
images/

# Preparation for the video walkthrough, which is mine and not part of the work.
docs/03-guia-de-defesa.md

# Local editor settings for this machine, not part of the delivery.
# `.claude/commands/` stays versioned: the /fim-de-fase command is part of the method.
.claude/settings.json
